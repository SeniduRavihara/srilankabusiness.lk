export default function ManageStorePage({ params }: { params: { storeId: string } }) {
  return (
    <div>
      <h1>Manage Business Profile for {params.storeId}</h1>
      {/* Template for ManageStorePage */}
    </div>
  );
}