export default function AddTabData({ params }: { params: { storeId: string } }) {
  return (
    <div>
      <h1>Setup Tabs Data for {params.storeId}</h1>
      {/* Template for AddTabData */}
    </div>
  );
}