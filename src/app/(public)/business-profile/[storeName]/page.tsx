export default function StoreDetailsPage({ params }: { params: { storeName: string } }) {
  return (
    <div>
      <h1>Business Profile for {params.storeName}</h1>
      {/* Template for StoreDetailsPage */}
    </div>
  );
}