export default function SearchResultsPage({ params }: { params: { category: string } }) {
  return (
    <div>
      <h1>Search Results for {params.category}</h1>
      {/* Template for SearchResultsPage */}
    </div>
  );
}