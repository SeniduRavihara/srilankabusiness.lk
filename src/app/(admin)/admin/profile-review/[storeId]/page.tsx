export default function ReviewPage({ params }: { params: { storeId: string } }) {
  return (
    <div>
      <h1>Profile Review for {params.storeId}</h1>
      {/* Template for ReviewPage */}
    </div>
  );
}